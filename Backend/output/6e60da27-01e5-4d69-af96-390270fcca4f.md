## Bellman Ford Algorithm

**Definition:** An algorithm for finding the shortest paths from a single source vertex to all other vertices in a weighted graph, capable of handling negative edge weights and detecting negative weight cycles.

**Formula:**
$$
\text{Time Complexity} = O(VE)\
$$

The Bellman-Ford algorithm operates by iteratively relaxing all edges in the graph. It initializes distances to all vertices as infinity except the source, which is set to zero. Through V-1 iterations, it updates the shortest path estimates by checking if traversing an edge provides a shorter path. After these iterations, if further relaxation is possible, it indicates the presence of a negative weight cycle. This algorithm is particularly useful in scenarios where negative weights are present but no negative cycles exist. Its ability to detect negative cycles makes it valuable in network routing and other applications requiring robust shortest path calculations.

**Algorithm Steps:**
1. Initialize distance to all vertices as infinity except source (0).
2. Relax all edges V-1 times.
3. Check for negative weight cycles by attempting one more relaxation.
4. If relaxation is possible, a negative cycle exists.
5. Return the shortest path distances.

```python
def bellman_ford(graph, source):
    distances = {node: float('inf') for node in graph}
    distances[source] = 0
    for _ in range(len(graph)-1):
        for u, v, w in graph:
            if distances[u] + w < distances[v]:
                distances[v] = distances[u] + w
    # Check for negative weight cycles
    for u, v, w in graph:
        if distances[u] + w < distances[v]:
            distances[v] = float('inf')
    return distances
```

> [!NOTE]
> **Diagram Description:** The diagram illustrates the step-by-step relaxation of edges in a graph, showing how distances to each vertex are updated iteratively until no further improvements are possible.

> **Example:** In a network of interconnected cities, the Bellman-Ford algorithm can determine the shortest route from a starting city to all other cities, even if some roads have negative weights representing discounts or special routes.

> [!NOTE]
> **Memory Trick:** Remember BELLMAN-FORD as B for Bellman, E for Edge, L for Relax, M for Multiple, F for Final, O for Optimization, R for Relaxation, D for Detection.

> [!WARNING]
> **Common Mistake:** Common mistakes include forgetting to run the algorithm for V-1 iterations, not checking for negative cycles, or misinterpreting the meaning of negative edge weights.

> [!TIP]
> **Exam Focus:** Focus on understanding the relaxation process and how negative cycles are detected. Practice problems involving graphs with negative edges and cycle detection.

**Key Takeaways:**
- Bellman-Ford handles negative edges and detects cycles.
- It runs in O(VE) time.
- It is suitable for networks with negative weights but no cycles.

